{
  "version": 2,
  "builds": [
    {
      "src": "next.config.js",
      "use": "@vercel/next",
      "config": {
        "zeroConfig": false
      }
    }
  ],
  "buildCommand": "npm install --legacy-peer-deps && npm run build"
}
